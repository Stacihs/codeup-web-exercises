function evenOrOdd(number) {
    if (!isNumber(number)) {
        return "That is not a number.";
    } else if (number % 2 === 0) {
        return "even";
    } else {
        return "odd";
    }
}

function isNumber(input) {
    if (input )
}